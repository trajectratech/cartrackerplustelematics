---
import { Icon } from "astro-icon/components";
import type { ThemeConfig } from "../types/theme";
import QuoteRequestForm from "./QuoteRequestForm.astro";
export interface Props {
	data: ThemeConfig;
}
const cta = Astro.props.data.content.ctaSection;
const whatsappUrl = Astro.props.data.content.heroSection?.ctaUrl ?? "#";
---

{
	cta && (
		<section id="get-a-quote" class="scrolling-section section-shell text-(--color-text)" aria-labelledby="get-a-quote-heading">
			<div class="container-shell">
				<div class="cta-shell p-8 sm:p-10 lg:p-14">
					<div class="grid items-start gap-10 lg:grid-cols-[0.9fr_1.1fr]">
						<div>
							<p class="section-kicker">Request Pricing</p>
							<h2 id="get-a-quote-heading" class="mt-7 max-w-4xl text-[clamp(2rem,3.4vw,3.4rem)] font-bold leading-[0.98] tracking-[-0.04em] text-[var(--color-heading)]">
								{cta.heading}
							</h2>
							{cta.subheading && (
								<p class="mt-5 max-w-2xl text-lg leading-8 text-(--color-text-muted)">
									{cta.subheading}
								</p>
							)}

							<div class="mt-9 grid gap-4">
								<a
									href={whatsappUrl}
									target="_blank"
									rel="noopener noreferrer"
									data-button
									class="primary-button inline-flex cursor-pointer items-center justify-center gap-2"
								>
									Chat Now on WhatsApp
									<Icon name="heroicons-solid:external-link" class="h-4 w-4" aria-hidden="true" />
								</a>
							</div>
						</div>

						<div class="premium-panel p-6 sm:p-8">
							<div class="mb-6">
								<p class="eyebrow-label">Web Estimate Request</p>
								<p class="mt-4 text-2xl font-bold tracking-[-0.03em] text-(--color-heading)">
									Tell us about your requirements once and our team will respond with a tailored proposal.
								</p>
							</div>
							<QuoteRequestForm formId="home-quote-form" sourcePage="/#get-a-quote" />
						</div>
					</div>
				</div>
			</div>
		</section>
	)
}
